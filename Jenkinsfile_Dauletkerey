pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                bat 'mvn clean package'
            }
        }

        stage('Test') {
            steps {
                bat 'mvn test'
            }
        }

        stage('Docker Build') {
            steps {
                bat 'docker build -f Dockerfile_Dauletkerey -t dauletkerey/todo-app:2.0 .'
            }
        }

        stage('Docker Login') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker-hub', usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                    bat """
                    docker logout || echo No previous login
                    docker login -u %USER% -p %PASS%
                    """
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                bat 'docker push dauletkerey/todo-app:2.0'
            }
        }
    }
}
